services:
  db:
    image: mysql:8.4
    container_name: restaurant_saas_db
    restart: unless-stopped
    environment:
<<<<<<< HEAD
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root_password}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-restaurant_saas}
      MYSQL_USER: ${MYSQL_USER:-restaurant_user}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-restaurant_password}
=======
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: restaurant_saas
      MYSQL_USER: restaurant_user
      MYSQL_PASSWORD: restaurant_password
>>>>>>> 12ae8a7538e7247857354f2c0c441e94a0eb39cf
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
    container_name: restaurant_saas_backend
    restart: unless-stopped
<<<<<<< HEAD
    env_file:
      - .env
    environment:
      MYSQL_HOST: db
      MYSQL_PORT: 3306
=======
    environment:
      MYSQL_USER: restaurant_user
      MYSQL_PASSWORD: restaurant_password
      MYSQL_HOST: db
      MYSQL_PORT: 3306
      MYSQL_DATABASE: restaurant_saas
      ENVIRONMENT: development
      SECRET_KEY: change-this-secret-in-production
      ACCESS_TOKEN_EXPIRE_MINUTES: 720
      CORS_ALLOWED_ORIGINS: http://localhost:5173,http://127.0.0.1:5173,http://localhost:5178,http://127.0.0.1:5178
      RETURN_DEV_RESET_TOKEN: "true"
      SUPERADMIN_EMAIL: superadmin@restaurant.test
      SUPERADMIN_USERNAME: superadmin
      SUPERADMIN_PASSWORD: Superadmin123!
>>>>>>> 12ae8a7538e7247857354f2c0c441e94a0eb39cf
    ports:
      - "8001:8000"
    volumes:
      - ./backend/uploads:/app/uploads
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
    container_name: restaurant_saas_frontend
    restart: unless-stopped
    environment:
      VITE_API_URL: http://localhost:8001
    ports:
      - "${FRONTEND_PORT:-5178}:5173"
    depends_on:
      - backend

volumes:
  mysql_data:
